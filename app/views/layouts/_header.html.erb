<div class="topbar">
  <div class="fill">
    <div class="container">
      <a href="<%= root_path %>" class="brand"><abbr>YATA</abbr> <span class="label success"><%= SEMVER %></span></a>
      <div class="auth pull-right">
        <% if current_user %>
            <%= link_to "Sign out", "/logout", class: "btn danger" %>
        <% end %>
      </div>
    </div>
  </div>
</div>